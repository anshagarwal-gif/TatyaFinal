# Server Configuration
server.port=8080
spring.application.name=tatya-backend

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tatyaapp
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# OTP Configuration
otp.expiry.minutes=5
otp.length=4

# SMS Configuration - MSG91 (Perfect for India!)
# Get your FREE API key from: https://msg91.com/
# 1. Sign up (free credits available)
# 2. Get Auth Key from dashboard
# 3. Add it below
# 4. Restart application
msg91.auth.key=482457ASF0bgry693a7090P1
msg91.sender.id=TATYA
msg91.route=4
msg91.country.code=91
sms.enabled=true



# Razorpay Configuration
# Get your keys from: https://dashboard.razorpay.com/app/keys
razorpay.key.id=rzp_test_placeholder
razorpay.key.secret=placeholder_secret

# File Upload Configuration
file.upload.dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Email Configuration (used when admin approves a vendor)
# Set mail.enabled=true and configure SMTP to actually send emails.
mail.enabled=true
# If true, prints the generated vendor password in backend logs (NOT recommended for production)
mail.log.credentials=true
mail.from=contact.tatya@gmail.com
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=contact.tatya@gmail.com
spring.mail.password=qlqv wpfs zwfu prjg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
